{
  "version": 2,
  "builds": [
    { "src": "client/*", "use": "@vercel/static-build" },
    { "src": "server/*", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/client/(.*)", "dest": "/client/$1" },
    { "src": "/server/(.*)", "dest": "/server/$1", "src": "/server/(.*)" }
  ]
}
